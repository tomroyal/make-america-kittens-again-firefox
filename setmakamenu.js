// setmakamenu.js - part of make america kittens again
// v1.1.3
// by Tom Royal
// tomroyal.com

// sets up a menu item to invoke the de-kittening
/*
function DeMakaMenuClick() {
  console.log("trying");
  for (let tab of tabs) {
    browser.tabs.sendMessage(
      tab.id,
      {message: "demaka"}
    ).then(response => {
      console.log("Message from the content script:");
      console.log(response.response);
    }).catch(onError);
  }
}


var setMAKAmenu1 = browser.contextMenus.create({"title": "Make Kittens Trump Again :(", "contexts":["page"], "onclick": DeMakaMenuClick});
*/
